# 复制为同目录下的 .env 后生效（已被根目录 .gitignore 忽略）
# 本地联调 openmcp-backend 默认用 development；要打远程正式环境改为 production
OPENMCP_APP_ENV=development

# 可选：直接指定完整 API 根（可带或不带 /api/v1，会自动规范化）
# OPENMCP_API_BASE_URL=http://localhost:8000
